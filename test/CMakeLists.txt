add_executable(test_spdlog test_spdlog.cpp)
target_link_libraries(test_spdlog PRIVATE video-pipeline::spdlog)
video_pipeline_copy_runtime_dlls(test_spdlog)
if(MSVC)
    target_compile_options(test_spdlog PRIVATE /W3 /utf-8 /Zc:__cplusplus)
else()
    target_compile_options(test_spdlog PRIVATE -Wall -Wextra)
endif()
add_test(NAME test_spdlog COMMAND test_spdlog)

add_executable(test_render_frame_converter
    render/test_frame_converter.cpp
    "${CMAKE_SOURCE_DIR}/src/render/frame_converter.cpp"
)
target_include_directories(test_render_frame_converter PRIVATE "${CMAKE_SOURCE_DIR}/include")
target_link_libraries(test_render_frame_converter PRIVATE video-pipeline::spdlog)
if(MSVC)
    target_compile_options(test_render_frame_converter PRIVATE /W3 /utf-8 /Zc:__cplusplus)
else()
    target_compile_options(test_render_frame_converter PRIVATE -Wall -Wextra)
endif()
add_test(NAME test_render_frame_converter COMMAND test_render_frame_converter)

add_executable(test_filter_audio_resample filter/test_audio_resample_filter.cpp)
target_link_libraries(test_filter_audio_resample PRIVATE video_pipeline_lib)
video_pipeline_copy_runtime_dlls(test_filter_audio_resample)
if(MSVC)
    target_compile_options(test_filter_audio_resample PRIVATE /W3 /utf-8 /Zc:__cplusplus)
else()
    target_compile_options(test_filter_audio_resample PRIVATE -Wall -Wextra)
endif()
add_test(NAME test_filter_audio_resample COMMAND test_filter_audio_resample)

if(VIDEO_PIPELINE_BUILD_RENDER)
    add_executable(test_video_pts_normalizer render/test_video_pts_normalizer.cpp)
    target_link_libraries(test_video_pts_normalizer PRIVATE video_pipeline_lib)
    video_pipeline_copy_runtime_dlls(test_video_pts_normalizer)
    if(MSVC)
        target_compile_options(test_video_pts_normalizer PRIVATE /W3 /utf-8 /Zc:__cplusplus)
    else()
        target_compile_options(test_video_pts_normalizer PRIVATE -Wall -Wextra)
    endif()
    add_test(NAME test_video_pts_normalizer COMMAND test_video_pts_normalizer)

    add_executable(test_av_sync_controller render/test_av_sync_controller.cpp)
    target_link_libraries(test_av_sync_controller PRIVATE video_pipeline_lib)
    video_pipeline_copy_runtime_dlls(test_av_sync_controller)
    if(MSVC)
        target_compile_options(test_av_sync_controller PRIVATE /W3 /utf-8 /Zc:__cplusplus)
    else()
        target_compile_options(test_av_sync_controller PRIVATE -Wall -Wextra)
    endif()
    add_test(NAME test_av_sync_controller COMMAND test_av_sync_controller)

    add_executable(test_audio_render_strategy render/test_audio_render_strategy.cpp)
    target_link_libraries(test_audio_render_strategy PRIVATE video_pipeline_lib)
    video_pipeline_copy_runtime_dlls(test_audio_render_strategy)
    if(MSVC)
        target_compile_options(test_audio_render_strategy PRIVATE /W3 /utf-8 /Zc:__cplusplus)
    else()
        target_compile_options(test_audio_render_strategy PRIVATE -Wall -Wextra)
    endif()
    add_test(NAME test_audio_render_strategy COMMAND test_audio_render_strategy)

    add_executable(test_render_session render/test_render_session.cpp)
    target_link_libraries(test_render_session PRIVATE video_pipeline_lib)
    video_pipeline_copy_runtime_dlls(test_render_session)
    if(MSVC)
        target_compile_options(test_render_session PRIVATE /W3 /utf-8 /Zc:__cplusplus)
    else()
        target_compile_options(test_render_session PRIVATE -Wall -Wextra)
    endif()
    add_test(NAME test_render_session COMMAND test_render_session)

    add_executable(test_opengl_video_renderer render/test_opengl_video_renderer.cpp)
    target_link_libraries(test_opengl_video_renderer PRIVATE video_pipeline_lib)
    video_pipeline_copy_runtime_dlls(test_opengl_video_renderer)
    if(MSVC)
        target_compile_options(test_opengl_video_renderer PRIVATE /W3 /utf-8 /Zc:__cplusplus)
    else()
        target_compile_options(test_opengl_video_renderer PRIVATE -Wall -Wextra)
    endif()
    add_test(NAME test_opengl_video_renderer COMMAND test_opengl_video_renderer --smoke)
    set_tests_properties(test_opengl_video_renderer PROPERTIES SKIP_RETURN_CODE 77)
endif()

add_executable(test_publisher_protocol media/test_publisher_protocol.cpp)
target_link_libraries(test_publisher_protocol PRIVATE video_pipeline_lib)
video_pipeline_copy_runtime_dlls(test_publisher_protocol)
if(MSVC)
    target_compile_options(test_publisher_protocol PRIVATE /W3 /utf-8 /Zc:__cplusplus)
else()
    target_compile_options(test_publisher_protocol PRIVATE -Wall -Wextra)
endif()
add_test(NAME test_publisher_protocol COMMAND test_publisher_protocol)

add_executable(test_rtsp_request_parser media/test_rtsp_request_parser.cpp)
target_link_libraries(test_rtsp_request_parser PRIVATE video_pipeline_lib)
video_pipeline_copy_runtime_dlls(test_rtsp_request_parser)
if(MSVC)
    target_compile_options(test_rtsp_request_parser PRIVATE /W3 /utf-8 /Zc:__cplusplus)
else()
    target_compile_options(test_rtsp_request_parser PRIVATE -Wall -Wextra)
endif()
add_test(NAME test_rtsp_request_parser COMMAND test_rtsp_request_parser)

add_executable(test_avtp_timestamp_mapper media/test_avtp_timestamp_mapper.cpp)
target_link_libraries(test_avtp_timestamp_mapper PRIVATE video_pipeline_lib)
video_pipeline_copy_runtime_dlls(test_avtp_timestamp_mapper)
if(MSVC)
    target_compile_options(test_avtp_timestamp_mapper PRIVATE /W3 /utf-8 /Zc:__cplusplus)
else()
    target_compile_options(test_avtp_timestamp_mapper PRIVATE -Wall -Wextra)
endif()
add_test(NAME test_avtp_timestamp_mapper COMMAND test_avtp_timestamp_mapper)

if(ENABLE_PCAP)
    add_executable(test_avtp_puller media/test_avtp_puller.cpp)
    target_link_libraries(test_avtp_puller PRIVATE video_pipeline_lib)
    video_pipeline_copy_runtime_dlls(test_avtp_puller)
    if(MSVC)
        target_compile_options(test_avtp_puller PRIVATE /W3 /utf-8 /Zc:__cplusplus)
    else()
        target_compile_options(test_avtp_puller PRIVATE -Wall -Wextra)
    endif()
    add_test(NAME test_avtp_puller COMMAND test_avtp_puller)

    add_executable(avtp_live_probe media/avtp_live_probe.cpp)
    target_link_libraries(avtp_live_probe PRIVATE video_pipeline_lib)
    video_pipeline_copy_runtime_dlls(avtp_live_probe)
    if(MSVC)
        target_compile_options(avtp_live_probe PRIVATE /W3 /utf-8 /Zc:__cplusplus)
    else()
        target_compile_options(avtp_live_probe PRIVATE -Wall -Wextra)
    endif()

    add_executable(avtp_decode_rtsp_publisher media/avtp_decode_rtsp_publisher.cpp)
    target_link_libraries(avtp_decode_rtsp_publisher PRIVATE video_pipeline_lib)
    video_pipeline_copy_runtime_dlls(avtp_decode_rtsp_publisher)
    if(MSVC)
        target_compile_options(avtp_decode_rtsp_publisher PRIVATE /W3 /utf-8 /Zc:__cplusplus)
    else()
        target_compile_options(avtp_decode_rtsp_publisher PRIVATE -Wall -Wextra)
    endif()

    if(VIDEO_PIPELINE_BUILD_RENDER)
        add_executable(avtp_render_probe media/avtp_render_probe.cpp)
        target_link_libraries(avtp_render_probe PRIVATE video_pipeline_lib)
        video_pipeline_copy_runtime_dlls(avtp_render_probe)
        if(MSVC)
            target_compile_options(avtp_render_probe PRIVATE /W3 /utf-8 /Zc:__cplusplus)
        else()
            target_compile_options(avtp_render_probe PRIVATE -Wall -Wextra)
        endif()
    endif()
endif()

if(VIDEO_PIPELINE_BUILD_INFERENCE)
    add_executable(test_inference_smoke inference/test_inference_smoke.cpp)
    target_link_libraries(test_inference_smoke PRIVATE video_pipeline_lib)
    video_pipeline_copy_runtime_dlls(test_inference_smoke)
    if(MSVC)
        target_compile_options(test_inference_smoke PRIVATE /W3 /utf-8 /Zc:__cplusplus)
    else()
        target_compile_options(test_inference_smoke PRIVATE -Wall -Wextra)
    endif()
    add_test(NAME test_inference_smoke COMMAND test_inference_smoke)

    add_executable(test_yolov5_inference inference/test_yolov5_inference.cpp)
    target_link_libraries(test_yolov5_inference PRIVATE video_pipeline_lib)
    target_compile_definitions(test_yolov5_inference
        PRIVATE
            VIDEO_PIPELINE_SOURCE_DIR="${CMAKE_SOURCE_DIR}"
    )
    video_pipeline_copy_runtime_dlls(test_yolov5_inference)
    if(MSVC)
        target_compile_options(test_yolov5_inference PRIVATE /W3 /utf-8 /Zc:__cplusplus)
    else()
        target_compile_options(test_yolov5_inference PRIVATE -Wall -Wextra)
    endif()
    add_test(NAME test_yolov5_inference COMMAND test_yolov5_inference)
endif()

add_executable(test_ffmpeg_audio_encoder media/test_ffmpeg_audio_encoder.cpp)
target_link_libraries(test_ffmpeg_audio_encoder PRIVATE video_pipeline_lib)
video_pipeline_copy_runtime_dlls(test_ffmpeg_audio_encoder)
if(MSVC)
    target_compile_options(test_ffmpeg_audio_encoder PRIVATE /W3 /utf-8 /Zc:__cplusplus)
else()
    target_compile_options(test_ffmpeg_audio_encoder PRIVATE -Wall -Wextra)
endif()
add_test(NAME test_ffmpeg_audio_encoder COMMAND test_ffmpeg_audio_encoder)

add_executable(test_ffmpeg_decoder_raw_packet media/test_ffmpeg_decoder_raw_packet.cpp)
target_link_libraries(test_ffmpeg_decoder_raw_packet PRIVATE video_pipeline_lib)
target_compile_definitions(test_ffmpeg_decoder_raw_packet
    PRIVATE
        VIDEO_PIPELINE_TEST_MP4_PATH="${CMAKE_SOURCE_DIR}/test.mp4"
)
video_pipeline_copy_runtime_dlls(test_ffmpeg_decoder_raw_packet)
if(MSVC)
    target_compile_options(test_ffmpeg_decoder_raw_packet PRIVATE /W3 /utf-8 /Zc:__cplusplus)
else()
    target_compile_options(test_ffmpeg_decoder_raw_packet PRIVATE -Wall -Wextra)
endif()
add_test(NAME test_ffmpeg_decoder_raw_packet COMMAND test_ffmpeg_decoder_raw_packet)

add_executable(test_rtsp_server_publisher media/test_rtsp_server_publisher.cpp)
target_link_libraries(test_rtsp_server_publisher PRIVATE video_pipeline_lib)
target_compile_definitions(test_rtsp_server_publisher
    PRIVATE
        VIDEO_PIPELINE_SOURCE_DIR="${CMAKE_SOURCE_DIR}"
)
if(TARGET video_pipeline_common_process)
    target_link_libraries(test_rtsp_server_publisher PRIVATE video_pipeline_common_process)
    target_compile_definitions(test_rtsp_server_publisher
        PRIVATE
            VIDEO_PIPELINE_HAS_COMMON_PROCESS=1
    )
endif()
video_pipeline_copy_runtime_dlls(test_rtsp_server_publisher)
if(MSVC)
    target_compile_options(test_rtsp_server_publisher PRIVATE /W3 /utf-8 /Zc:__cplusplus)
else()
    target_compile_options(test_rtsp_server_publisher PRIVATE -Wall -Wextra)
endif()
add_executable(test_local_mp4_decode_rtsp_publisher media/test_local_mp4_decode_rtsp_publisher.cpp)
target_link_libraries(test_local_mp4_decode_rtsp_publisher PRIVATE video_pipeline_lib)
target_compile_definitions(test_local_mp4_decode_rtsp_publisher
    PRIVATE
        VIDEO_PIPELINE_TEST_MP4_PATH="${CMAKE_SOURCE_DIR}/test.mp4"
)
video_pipeline_copy_runtime_dlls(test_local_mp4_decode_rtsp_publisher)
if(MSVC)
    target_compile_options(test_local_mp4_decode_rtsp_publisher PRIVATE /W3 /utf-8 /Zc:__cplusplus)
else()
    target_compile_options(test_local_mp4_decode_rtsp_publisher PRIVATE -Wall -Wextra)
endif()
add_test(
    NAME test_local_mp4_decode_rtsp_publisher
    COMMAND test_local_mp4_decode_rtsp_publisher
        --once
        --self-test
        --port 0
        --max-video-packets 300
        --max-decoded-frames 120
        --no-realtime
)

add_executable(test_stream_decode media/test_stream_decode.cpp)
target_link_libraries(test_stream_decode PRIVATE video_pipeline_lib)
video_pipeline_copy_runtime_dlls(test_stream_decode)
if(MSVC)
    target_compile_options(test_stream_decode PRIVATE /W3 /utf-8 /Zc:__cplusplus)
else()
    target_compile_options(test_stream_decode PRIVATE -Wall -Wextra)
endif()

if(TARGET video_pipeline_common_process)
    add_executable(test_zlmediakit_process media/test_zlmediakit_process.cpp)
    target_link_libraries(test_zlmediakit_process PRIVATE video_pipeline_common_process)
    video_pipeline_copy_runtime_dlls(test_zlmediakit_process)
    if(MSVC)
        target_compile_options(test_zlmediakit_process PRIVATE /W3 /utf-8 /Zc:__cplusplus)
    else()
        target_compile_options(test_zlmediakit_process PRIVATE -Wall -Wextra)
    endif()

    add_executable(test_rtsp_decode_encode_push_zlm media/test_rtsp_decode_encode_push_zlm.cpp)
    target_link_libraries(test_rtsp_decode_encode_push_zlm
        PRIVATE
            video_pipeline_lib
            video_pipeline_common_process
    )
    video_pipeline_copy_runtime_dlls(test_rtsp_decode_encode_push_zlm)
    if(MSVC)
        target_compile_options(test_rtsp_decode_encode_push_zlm PRIVATE /W3 /utf-8 /Zc:__cplusplus)
    else()
        target_compile_options(test_rtsp_decode_encode_push_zlm PRIVATE -Wall -Wextra)
    endif()
endif()
